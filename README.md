# qq_export

LiteLoaderQQNT plugin for macOS. Version: 1.0.0.

- JSONL uses QCE chunked layout: `manifest.json` + `chunks/chunk_0001.jsonl`.
- Logs and progress are updated during history reading and chunk writing.
- Chunk files are written only after a chunk is complete.
